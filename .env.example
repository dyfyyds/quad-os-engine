# 复制为 .env 后按需修改

# 前端脚手架整包（docker-compose.yml）宿主机端口
WEB_PORT=8088

# 以下仅完整栈（docker-compose.full.yml）使用
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=quad_os
MYSQL_USER=quados
MYSQL_PASSWORD=quados

# 宿主机端口
MYSQL_PORT=3307
BACKEND_PORT=8080
NGINX_PORT=8088
